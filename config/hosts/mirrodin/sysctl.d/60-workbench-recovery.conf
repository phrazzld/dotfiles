# Managed by Workbench: scripts/setup-recovery.sh
# Enable sync (16), remount read-only (32), and reboot/poweroff (128).
# Emergency sequence: S (sync), U (remount read-only), B (reboot).
kernel.sysrq = 176
